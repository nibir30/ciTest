on:
  workflow_dispatch:
jobs:
  use_api:
    runs-on: ubuntu-latest
    permissions:
      issues: read
    steps:
      - env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          curl --request GET \
          --url "https://api.github.com/nibir30/ciTest/dispatches" \
          --header "Accept: application/vnd.github+json" \
          --header "Authorization: Bearer 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"
      - run: flutter pub get
      - run: flutter clean
      - run: flutter build apk --release
